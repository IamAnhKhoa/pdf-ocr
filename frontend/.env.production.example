# Copy file này thành .env.production và điền URL Worker của bạn
# Sau khi deploy backend (Cloudflare Worker), thay bằng URL thực tế

VITE_API_URL=https://your-worker-name.your-subdomain.workers.dev/api/extract
